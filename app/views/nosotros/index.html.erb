<p id="notice"><%= notice %></p>

<h1>Listing Nosotros</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descipcion</th>
      <th>Foto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @nosotros.each do |nosotro| %>
      <tr>
        <td><%= nosotro.nombre %></td>
        <td><%= nosotro.descipcion %></td>
        <td><%= nosotro.foto %></td>
        <td><%= link_to 'Show', nosotro %></td>
        <td><%= link_to 'Edit', edit_nosotro_path(nosotro) %></td>
        <td><%= link_to 'Destroy', nosotro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Nosotro', new_nosotro_path %>
